import { Inter as interFont, Raleway as ralewayFont } from 'next/font/google';

export const inter = interFont({ subsets: ['latin'] });
export const raleway = ralewayFont({ subsets: ['latin'], weight: ['700'] });
